---
phase: 05-validate-and-activate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - promaia.config.json
  - prompts/agent_morning_briefing.md
  - prompts/agent_email_triage.md
  - prompts/agent_evening_digest.md
autonomous: true
requirements:
  - VALID-01
  - VALID-04

must_haves:
  truths:
    - "Email-triage runs 3x/day instead of every 2 hours"
    - "Agent prompts instruct explicit empty state reporting"
    - "Agent prompts forbid fabricating facts when data is missing"
    - "All three agents complete an SDK run without errors"
  artifacts:
    - path: "promaia.config.json"
      provides: "Agent schedule configuration"
      contains: "email-triage"
    - path: "prompts/agent_morning_briefing.md"
      provides: "Morning briefing prompt with grounding rules"
      contains: "no new"
    - path: "prompts/agent_email_triage.md"
      provides: "Email triage prompt with grounding rules"
      contains: "no new"
    - path: "prompts/agent_evening_digest.md"
      provides: "Evening digest prompt with grounding rules"
      contains: "no new"
  key_links:
    - from: "promaia.config.json"
      to: "promaia/agents/scheduler.py"
      via: "AgentConfig.interval_minutes read by scheduler loop"
      pattern: "interval_minutes"
    - from: "prompts/agent_morning_briefing.md"
      to: "promaia/agents/executor.py"
      via: "_load_custom_prompt reads prompt_file path"
      pattern: "prompt_file"
---

<objective>
Update agent schedules and harden prompts to prevent hallucination and ensure correct operational behavior.

Purpose: The email-triage agent runs every 2 hours but Zack is not mail-driven -- 3x/day is sufficient. All three agent prompts lack grounding rules, which caused the first agent run to fabricate facts (claimed previous runs existed, cited fabricated costs). Adding explicit empty-state reporting and anti-hallucination directives prevents this.

Output: Updated promaia.config.json with new email-triage interval, and three hardened agent prompt files with grounding rules.
</objective>

<execution_context>
@C:/Users/Zachary Turner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Zachary Turner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-validate-and-activate/05-CONTEXT.md

@promaia.config.json
@prompts/agent_morning_briefing.md
@prompts/agent_email_triage.md
@prompts/agent_evening_digest.md
@promaia/agents/agent_config.py
@promaia/agents/scheduler.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update email-triage schedule to 3x/day</name>
  <files>promaia.config.json</files>
  <action>
In `promaia.config.json`, update the `email-triage` agent configuration:

1. Change `"interval_minutes": 120` to `"interval_minutes": 480` (8 hours = 3x/day coverage).

The scheduler uses `interval_minutes` in `_run_agent_loop()` to sleep between runs. With 480 minutes, the agent runs roughly 3x per day when the scheduler is started in the morning. This matches the user decision: "Run email checks 3x/day: morning, midday, and afternoon."

Note: The `schedule` field exists in `AgentConfig` for future time-of-day scheduling (`[("daily", "06:00"), ("daily", "12:00"), ("daily", "16:00")]`), but the scheduler currently only reads `interval_minutes` (scheduler.py line 74: `interval_seconds = agent.interval_minutes * 60`). Using interval_minutes for now; Phase 6+ can add proper cron-style scheduling.

Do NOT change morning-briefing (1440 min / 24h) or evening-digest (1440 min / 24h) -- those are correct per CONTEXT.md.
  </action>
  <verify>
    <automated>cd "C:/Users/Zachary Turner/dev/promaia" && python -c "
import json
with open('promaia.config.json') as f:
    config = json.load(f)
for agent in config['agents']:
    if agent['name'] == 'email-triage':
        assert agent['interval_minutes'] == 480, f'Expected 480, got {agent[\"interval_minutes\"]}'
        print(f'OK: email-triage interval_minutes = {agent[\"interval_minutes\"]} (3x/day)')
    elif agent['name'] == 'morning-briefing':
        assert agent['interval_minutes'] == 1440, f'morning-briefing should be 1440'
        print(f'OK: morning-briefing interval = {agent[\"interval_minutes\"]}')
    elif agent['name'] == 'evening-digest':
        assert agent['interval_minutes'] == 1440, f'evening-digest should be 1440'
        print(f'OK: evening-digest interval = {agent[\"interval_minutes\"]}')
print('All schedule checks passed')
"</automated>
  </verify>
  <done>email-triage interval_minutes is 480 (3x/day). morning-briefing and evening-digest remain at 1440 (24h).</done>
</task>

<task type="auto">
  <name>Task 2: Add grounding rules and empty-state reporting to all agent prompts</name>
  <files>prompts/agent_morning_briefing.md, prompts/agent_email_triage.md, prompts/agent_evening_digest.md</files>
  <action>
Add a `## Grounding Rules` section to each of the three agent prompt files. This section MUST appear near the top (after the first paragraph, before the "What to Include" section). The rules are the same across all three prompts but customized for each agent's domain.

**Common grounding rules to add to ALL three prompts:**

```markdown
## Grounding Rules

CRITICAL: These rules override all other instructions.

1. **Only reference data you actually received in your context.** If an email, calendar event, memory, or action appears in your context data, you may reference it. If it does not appear, it does not exist for this run.

2. **Never fabricate.** Do not invent email subjects, sender names, calendar events, costs, previous run results, or any other facts. If you are uncertain whether something is real, do not include it.

3. **Report empty states explicitly.** When a data source has no items:
   - Say "No new emails since last check" -- not silence.
   - Say "No calendar events today" -- not skip the section.
   - Say "No pending actions" -- not omit the section.
   Knowing something was checked and found empty is valuable.

4. **When data is missing or errors occurred**, say so clearly: "Email context was not available for this run" or "Calendar data could not be loaded." Never fill gaps with fabricated content.

5. **Do not reference previous runs** unless previous run data is explicitly provided in your context. You have no memory of prior executions.
```

**Per-prompt customizations:**

For `agent_morning_briefing.md`:
- Add to the Calendar section: "If no calendar data is provided in context, state: 'Calendar data not available for this briefing.'"
- Add to the Email section: "If no email data is provided in context, state: 'No email context loaded for this briefing.'"

For `agent_email_triage.md`:
- Add to the "What to Do" section: "If no unread emails are found, report: 'No new emails requiring attention.' Do not manufacture email summaries."
- Add after Classify: "Only classify emails that appear in your context data. Do not reference emails you have not seen."

For `agent_evening_digest.md`:
- Add to Day Summary: "Only summarize events and actions that appear in your context data. If no activity data is available, state: 'No activity data available for today's digest.'"
- Add to Tonight's Suggestions: "Base suggestions only on project contexts and actions provided in your data. Do not invent project states."

Keep existing prompt content intact. Only ADD the grounding rules section and the per-section clarifications. Do not remove or rewrite existing instructions.
  </action>
  <verify>
    <automated>cd "C:/Users/Zachary Turner/dev/promaia" && python -c "
import os
prompts = ['prompts/agent_morning_briefing.md', 'prompts/agent_email_triage.md', 'prompts/agent_evening_digest.md']
for p in prompts:
    with open(p) as f:
        content = f.read()
    assert 'Grounding Rules' in content, f'{p} missing Grounding Rules section'
    assert 'Never fabricate' in content, f'{p} missing anti-fabrication rule'
    assert 'empty' in content.lower() or 'no new' in content.lower(), f'{p} missing empty state guidance'
    assert 'previous run' in content.lower(), f'{p} missing previous run rule'
    print(f'OK: {p} has grounding rules')
print('All prompt checks passed')
"</automated>
  </verify>
  <done>All three agent prompts contain Grounding Rules section with anti-hallucination directives, explicit empty-state reporting instructions, and per-section data-availability guidance.</done>
</task>

</tasks>

<verification>
1. promaia.config.json has email-triage at 480-minute interval
2. All three prompt files contain "Grounding Rules" section
3. Each prompt instructs agents to report empty states explicitly
4. Each prompt forbids referencing data not in context
5. Prompt files load correctly via executor._load_custom_prompt()
</verification>

<success_criteria>
- email-triage runs 3x/day (480 min interval) per user decision
- All three agent prompts contain anti-hallucination grounding rules
- Prompts instruct explicit empty-state reporting ("No new emails" not silence)
- Existing prompt content preserved -- only additions, no removals
</success_criteria>

<output>
After completion, create `.planning/phases/05-validate-and-activate/05-02-SUMMARY.md`
</output>
