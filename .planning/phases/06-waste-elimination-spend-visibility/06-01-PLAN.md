---
phase: 06-waste-elimination-spend-visibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - promaia/agents/model_router.py
  - promaia/agents/cost_tracker.py
  - promaia/brain/schema.sql
  - promaia/agents/agent_config.py
autonomous: true
requirements: [ROUTE-01, COST-01, COST-04]

must_haves:
  truths:
    - "ModelRouter maps every task type to a specific Gemini model with pricing metadata"
    - "CostTracker logs every API call with model, tokens, cached tokens, thinking tokens, and dollar cost to brain.agent_costs"
    - "Each agent has an assigned model in its config (morning-briefing: gemini-3-flash-preview, email-triage: gemini-3-flash-preview, evening-digest: gemini-3-flash-preview)"
    - "Cost computation uses model-aware pricing from ModelConfig, not hardcoded Claude rates"
  artifacts:
    - path: "promaia/agents/model_router.py"
      provides: "TaskType enum, ModelConfig dataclass, MODELS registry, TASK_MODEL_MAP, ModelRouter class"
      min_lines: 80
    - path: "promaia/agents/cost_tracker.py"
      provides: "CostRecord dataclass, CostTracker class with log_call and compute_cost"
      min_lines: 60
    - path: "promaia/brain/schema.sql"
      provides: "brain.agent_costs table definition with indexes"
      contains: "brain.agent_costs"
    - path: "promaia/agents/agent_config.py"
      provides: "model field on AgentConfig"
      contains: "model"
  key_links:
    - from: "promaia/agents/cost_tracker.py"
      to: "promaia/agents/model_router.py"
      via: "CostTracker.compute_cost takes ModelConfig"
      pattern: "ModelConfig"
    - from: "promaia/agents/cost_tracker.py"
      to: "brain.agent_costs"
      via: "INSERT INTO brain.agent_costs"
      pattern: "brain\\.agent_costs"
    - from: "promaia/agents/model_router.py"
      to: "promaia/ai/models.py"
      via: "Uses model IDs from GOOGLE_MODELS"
      pattern: "gemini-3-flash-preview|gemini-3.1"
---

<objective>
Create the model routing infrastructure, cost tracking table, and pricing computation that all subsequent plans depend on.

Purpose: Establish the foundation for model-aware execution -- every API call must know which model to use and how much it costs. This replaces the hardcoded Claude Sonnet pricing ($3/$15) with accurate Gemini model-specific pricing.

Output: ModelRouter class, CostTracker class, brain.agent_costs table, model field on AgentConfig.
</objective>

<execution_context>
@C:/Users/Zachary Turner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Zachary Turner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-waste-elimination-spend-visibility/06-RESEARCH.md

@promaia/ai/models.py
@promaia/agents/agent_config.py
@promaia/agents/execution_tracker.py
@promaia/brain/schema.sql
@promaia/storage/postgres_db.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ModelRouter with TaskType enum, ModelConfig dataclass, and routing table</name>
  <files>promaia/agents/model_router.py</files>
  <action>
Create `promaia/agents/model_router.py` with:

1. **TaskType enum** with values: classify, extract, embed, synthesize, reason, create, heartbeat.

2. **ModelConfig dataclass** with fields:
   - `model_id: str` (exact Gemini model ID from promaia/ai/models.py GOOGLE_MODELS)
   - `display_name: str`
   - `input_price_per_m: float` ($ per 1M input tokens)
   - `output_price_per_m: float` ($ per 1M output tokens)
   - `thinking_price_per_m: float` ($ per 1M thinking tokens -- billed as output)
   - `cached_input_discount: float` (multiplier, e.g. 0.1 = 90% off)
   - `min_cache_tokens: int` (minimum tokens for explicit caching)
   - `max_thinking_budget: int` (0 = disable thinking)
   - `temperature: float` (default 1.0 -- per user decision, must stay 1.0 for Gemini 3)

3. **MODELS dict** mapping string keys to ModelConfig instances. Use model IDs from promaia/ai/models.py GOOGLE_MODELS. Per user decision, use LATEST models, NOT 2.5 family:
   - `"flash"`: model_id=`"gemini-3-flash-preview"`, input=$0.15/M, output=$0.60/M, thinking=$0.60/M (billed as output), cached_discount=0.1, min_cache=1024, thinking_budget=8192, temp=1.0
   - `"flash-lite"`: model_id=`"gemini-3.1-flash-lite-preview"`, input=$0.04/M, output=$0.15/M, thinking=$0.00/M, cached_discount=0.1, min_cache=1024, thinking_budget=0 (per user decision: set thinking_budget=0 for Flash-Lite), temp=1.0
   - `"pro"`: model_id=`"gemini-3.1-pro-preview"`, input=$1.25/M, output=$10.00/M, thinking=$10.00/M, cached_discount=0.1, min_cache=4096, thinking_budget=0, temp=1.0
   - `"embedding"`: model_id=`"gemini-embedding-001"`, input=$0.00, output=$0.00, no caching, no thinking

   NOTE: Pricing figures above are approximate. Include a comment noting prices should be verified against current Google AI pricing page. The structure matters more than exact cents.

4. **TASK_MODEL_MAP** mapping TaskType to model key:
   - CLASSIFY -> "flash-lite"
   - EXTRACT -> "flash-lite"
   - EMBED -> "embedding"
   - SYNTHESIZE -> "flash"
   - REASON -> "pro"
   - CREATE -> "flash"
   - HEARTBEAT -> "flash-lite"

5. **AGENT_MODEL_MAP** mapping agent names to model keys. Per user decision (COST-04):
   - "morning-briefing" -> "flash" (Gemini 3 Flash for synthesis tasks)
   - "email-triage" -> "flash" (Gemini 3 Flash for agent tasks)
   - "evening-digest" -> "flash" (Gemini 3 Flash for agent tasks)

   NOTE: Research originally said Pro for morning-briefing, but user decision in additional_context says "gemini-3-flash-preview for agent tasks (briefing, triage, digest)". Honor user decision.

6. **ModelRouter class** with methods:
   - `get_model(task_type: TaskType) -> ModelConfig` -- lookup from TASK_MODEL_MAP
   - `get_agent_model(agent_name: str) -> ModelConfig` -- lookup from AGENT_MODEL_MAP, default to "flash"
   - `get_fallback_model(model_key: str) -> Optional[ModelConfig]` -- returns next tier up (flash-lite -> flash -> pro -> None)
  </action>
  <verify>
    <automated>cd "$HOME/dev/promaia" && python -c "
from promaia.agents.model_router import TaskType, ModelConfig, ModelRouter, MODELS, TASK_MODEL_MAP, AGENT_MODEL_MAP
router = ModelRouter()
# Verify all task types route
for tt in TaskType:
    m = router.get_model(tt)
    assert m.model_id, f'{tt} has no model_id'
# Verify agent routing
for name in ['morning-briefing', 'email-triage', 'evening-digest']:
    m = router.get_agent_model(name)
    assert 'flash' in m.model_id.lower() or 'pro' in m.model_id.lower(), f'{name} routes to unexpected model: {m.model_id}'
# Verify fallback chain
fb = router.get_fallback_model('flash-lite')
assert fb and fb.model_id == MODELS['flash'].model_id, 'flash-lite should fallback to flash'
fb2 = router.get_fallback_model('flash')
assert fb2 and fb2.model_id == MODELS['pro'].model_id, 'flash should fallback to pro'
assert router.get_fallback_model('pro') is None, 'pro should have no fallback'
# Verify thinking_budget=0 for flash-lite
assert MODELS['flash-lite'].max_thinking_budget == 0, 'flash-lite thinking must be 0'
print('All model_router checks passed')
"
    </automated>
  </verify>
  <done>ModelRouter correctly maps all 7 task types and 3 agents to Gemini models with pricing metadata, fallback chain works (flash-lite -> flash -> pro), thinking_budget=0 on Flash-Lite per user decision.</done>
</task>

<task type="auto">
  <name>Task 2: Create CostTracker, brain.agent_costs table, and add model field to AgentConfig</name>
  <files>promaia/agents/cost_tracker.py, promaia/brain/schema.sql, promaia/agents/agent_config.py</files>
  <action>
**A) Create `promaia/agents/cost_tracker.py`:**

1. **CostRecord dataclass** with fields: agent_name, model_id, task_type, input_tokens, output_tokens, cached_tokens, thinking_tokens, cost_usd, execution_id (int), created_at (datetime).

2. **CostTracker class** with:
   - `__init__()`: Calls `_ensure_table()` to create brain.agent_costs if not exists.
   - `_ensure_table()`: Uses `pg_connect()` from `promaia.storage.postgres_db` to run CREATE TABLE IF NOT EXISTS for brain.agent_costs and its indexes. Follow the exact pattern in execution_tracker.py for connection management.
   - `log_call(record: CostRecord)`: INSERT into brain.agent_costs. Use `%s` parameter substitution (psycopg2 style, matching existing codebase patterns).
   - `compute_cost(model_config: ModelConfig, usage: dict) -> float`: Takes a ModelConfig (from model_router.py) and a usage dict with keys: input_tokens, output_tokens, cached_tokens, thinking_tokens. Computes:
     - non_cached_input = input_tokens - cached_tokens
     - input_cost = non_cached_input * input_price_per_m / 1_000_000
     - cached_cost = cached_tokens * input_price_per_m * cached_input_discount / 1_000_000
     - output_cost = output_tokens * output_price_per_m / 1_000_000
     - thinking_cost = thinking_tokens * thinking_price_per_m / 1_000_000
     - Return sum. (Thinking tokens billed separately per research -- usage_metadata.thoughts_token_count)
   - `get_daily_spend() -> float`: SUM(cost_usd) WHERE created_at >= CURRENT_DATE.
   - `get_run_spend(execution_id: int) -> float`: SUM(cost_usd) WHERE execution_id = %s.
   - `get_daily_summary(days: int = 7) -> list[dict]`: GROUP BY date, agent_name with COUNT, SUM tokens, SUM cost. Return list of dicts.

**B) Update `promaia/brain/schema.sql`:**

Append the brain.agent_costs table definition after existing tables. Include:
```sql
CREATE TABLE IF NOT EXISTS brain.agent_costs (
    id SERIAL PRIMARY KEY,
    execution_id INTEGER REFERENCES agent_executions(id),
    agent_name TEXT NOT NULL,
    model_id TEXT NOT NULL,
    task_type TEXT,
    input_tokens INTEGER DEFAULT 0,
    output_tokens INTEGER DEFAULT 0,
    cached_tokens INTEGER DEFAULT 0,
    thinking_tokens INTEGER DEFAULT 0,
    cost_usd REAL NOT NULL,
    created_at TIMESTAMPTZ DEFAULT NOW()
);
CREATE INDEX IF NOT EXISTS idx_agent_costs_agent ON brain.agent_costs (agent_name, created_at DESC);
CREATE INDEX IF NOT EXISTS idx_agent_costs_date ON brain.agent_costs (created_at DESC);
CREATE INDEX IF NOT EXISTS idx_agent_costs_execution ON brain.agent_costs (execution_id);
```

**C) Update `promaia/agents/agent_config.py`:**

Add `model: Optional[str] = None` field to AgentConfig dataclass (after `sdk_permission_mode`). This is the model key from AGENT_MODEL_MAP (e.g., "flash", "pro"). When None, ModelRouter.get_agent_model() uses the default for that agent name. This field lets promaia.config.json override the default model per agent.
  </action>
  <verify>
    <automated>cd "$HOME/dev/promaia" && python -c "
from promaia.agents.cost_tracker import CostRecord, CostTracker
from promaia.agents.model_router import MODELS
from datetime import datetime, timezone

# Test cost computation
tracker = CostTracker()
flash = MODELS['flash']
cost = tracker.compute_cost(flash, {
    'input_tokens': 1000,
    'output_tokens': 500,
    'cached_tokens': 200,
    'thinking_tokens': 100,
})
assert cost > 0, f'Cost should be positive, got {cost}'
assert isinstance(cost, float), 'Cost should be float'

# Test zero-cost case
zero_cost = tracker.compute_cost(flash, {
    'input_tokens': 0,
    'output_tokens': 0,
    'cached_tokens': 0,
    'thinking_tokens': 0,
})
assert zero_cost == 0.0, f'Zero usage should be zero cost, got {zero_cost}'

# Test AgentConfig has model field
from promaia.agents.agent_config import AgentConfig
ac = AgentConfig(name='test', workspace='test', databases=[], prompt_file='', mcp_tools=[], max_iterations=1, output_notion_page_id='', model='flash')
assert ac.model == 'flash', 'model field not working'

print('All cost_tracker and agent_config checks passed')
"
    </automated>
  </verify>
  <done>CostTracker computes accurate model-aware costs, brain.agent_costs table is auto-created on init, AgentConfig has optional model field for per-agent model override.</done>
</task>

</tasks>

<verification>
- `python -c "from promaia.agents.model_router import ModelRouter; print('ModelRouter importable')"` succeeds
- `python -c "from promaia.agents.cost_tracker import CostTracker; print('CostTracker importable')"` succeeds
- brain.agent_costs table exists in Postgres after CostTracker() instantiation
- ModelRouter maps all 7 TaskType values and all 3 agent names
- Fallback chain: flash-lite -> flash -> pro -> None
- Cost computation handles cached tokens, thinking tokens, and zero-usage edge case
</verification>

<success_criteria>
1. ModelRouter returns correct Gemini models for every task type and agent name
2. CostTracker can compute costs using ModelConfig pricing (not hardcoded Claude rates)
3. brain.agent_costs table auto-creates with proper schema and indexes
4. AgentConfig has model field that can be set in promaia.config.json
5. No hardcoded $3/$15 Claude pricing in any new code
</success_criteria>

<output>
After completion, create `.planning/phases/06-waste-elimination-spend-visibility/06-01-SUMMARY.md`
</output>
