---
phase: 06-waste-elimination-spend-visibility
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - promaia/agents/agent_context.py
  - prompts/agent_morning_briefing.md
  - prompts/agent_email_triage.md
  - prompts/agent_evening_digest.md
autonomous: true
requirements: [ROUTE-03, ROUTE-04, ROUTE-05]

must_haves:
  truths:
    - "AgentContext loads user profile, pending actions, active projects, and temporal awareness from brain"
    - "Agent prompts have constraints at TOP of system instruction, not bottom"
    - "Each agent prompt only includes tool documentation relevant to that agent"
    - "Agent prompts use consistent Markdown structure (no mixed XML/Markdown per Gemini requirement)"
  artifacts:
    - path: "promaia/agents/agent_context.py"
      provides: "AgentContext dataclass with to_prompt_block() rendering"
      min_lines: 60
    - path: "prompts/agent_morning_briefing.md"
      provides: "Restructured prompt with constraints-first, few-shot example"
      contains: "Grounding Rules"
    - path: "prompts/agent_email_triage.md"
      provides: "Restructured prompt with constraints-first, few-shot example"
      contains: "Grounding Rules"
    - path: "prompts/agent_evening_digest.md"
      provides: "Restructured prompt with constraints-first, few-shot example"
      contains: "Grounding Rules"
  key_links:
    - from: "promaia/agents/agent_context.py"
      to: "promaia/storage/postgres_db.py"
      via: "Queries brain.profile, brain.actions, brain.contexts"
      pattern: "brain\\.profile|brain\\.actions"
    - from: "promaia/agents/agent_context.py"
      to: "prompts/agent_*.md"
      via: "to_prompt_block() output injected as context section"
      pattern: "to_prompt_block"
---

<objective>
Create the AgentContext dataclass for standardized agent awareness and restructure all three agent prompts for optimal Gemini performance.

Purpose: Agents need consistent context (who Zack is, what time it is, what's pending) and prompts need restructuring per Gemini best practices (constraints at top, few-shot > verbose, Markdown-only structure). This is independent of the model routing work in Plan 01 -- it's purely about data and prompts.

Output: AgentContext dataclass, 3 restructured agent prompts.
</objective>

<execution_context>
@C:/Users/Zachary Turner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Zachary Turner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-waste-elimination-spend-visibility/06-RESEARCH.md

@prompts/agent_morning_briefing.md
@prompts/agent_email_triage.md
@prompts/agent_evening_digest.md
@promaia/brain/schema.sql
@promaia/storage/postgres_db.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AgentContext dataclass with brain data loading and prompt rendering</name>
  <files>promaia/agents/agent_context.py</files>
  <action>
Create `promaia/agents/agent_context.py` with:

1. **AgentContext dataclass** with fields:
   - `user_name: str` -- from brain.profile (key="name")
   - `user_profile_summary: str` -- ~200 token summary from brain.profile (top traits by confidence)
   - `current_time: datetime`
   - `day_of_week: str`
   - `is_office_day: bool` -- inferred from profile (Zack's schedule data)
   - `pending_actions: list[dict]` -- from brain.actions WHERE status='pending'
   - `active_projects: list[dict]` -- from brain.contexts WHERE type='project' AND status='active'
   - `recent_memories: list[dict]` -- last 5 memories from brain.memories ORDER BY created_at DESC
   - `domain_state: dict` -- caller-provided dict (e.g., {"unread_emails": 5})

2. **`to_prompt_block() -> str`** method that renders the context as a text block (~200-300 tokens). Format:
   ```
   ## User Context
   Current time: 2026-03-06 14:30 EST (Thursday)
   User: Zack
   Profile: [summary of top 5-8 profile traits]
   Office day: Yes/No
   Pending actions: N items
   Active projects: project1, project2
   Recent activity: [last 2-3 memory summaries]
   ```

3. **`@classmethod load_from_brain(cls, domain_state: dict = None) -> AgentContext`** -- class method that:
   - Uses `pg_connect()` from `promaia.storage.postgres_db` to query brain tables
   - Loads user name from `brain.profile WHERE key = 'name'`
   - Builds profile summary from `brain.profile ORDER BY confidence DESC LIMIT 10` -- concatenates key/value pairs into a readable sentence
   - Queries `brain.actions WHERE status = 'pending' ORDER BY created_at DESC LIMIT 5`
   - Queries `brain.contexts WHERE status = 'active' LIMIT 5`
   - Queries `brain.memories ORDER BY created_at DESC LIMIT 5`
   - Handles missing data gracefully (empty lists, "Unknown" for name if not found)
   - Sets current_time, day_of_week, is_office_day from datetime.now()

4. **`get_agent_tools_docs(agent_name: str) -> str`** -- standalone function (ROUTE-04) that returns only the MCP tool documentation relevant to this agent. Use the agent's `mcp_tools` list from AgentConfig:
   - morning-briefing: brain_search, brain_context, profile
   - email-triage: brain_search, gmail-related tools
   - evening-digest: brain_search, brain_context, brain_actions

   For now, return a formatted string listing the tool names and one-line descriptions. The actual tool schemas are loaded at runtime by the executor -- this just controls which tool docs go into the system prompt to reduce prompt size. If tool docs are not yet available for a tool name, output the tool name with "Available at runtime" description.
  </action>
  <verify>
    <automated>cd "$HOME/dev/promaia" && python -c "
from promaia.agents.agent_context import AgentContext, get_agent_tools_docs
from datetime import datetime, timezone

# Test manual construction (no DB needed)
ctx = AgentContext(
    user_name='Zack',
    user_profile_summary='Software developer, prefers directness',
    current_time=datetime.now(timezone.utc),
    day_of_week='Thursday',
    is_office_day=False,
    pending_actions=[],
    active_projects=[{'name': 'Promaia'}],
    recent_memories=[],
    domain_state={'unread_emails': 3}
)
block = ctx.to_prompt_block()
assert 'Zack' in block, 'User name missing from prompt block'
assert 'Promaia' in block, 'Project name missing from prompt block'
assert len(block) > 50, f'Prompt block too short: {len(block)} chars'
assert len(block) < 2000, f'Prompt block too long: {len(block)} chars'

# Test tool docs
tools = get_agent_tools_docs('morning-briefing')
assert isinstance(tools, str), 'Tool docs should be string'
assert len(tools) > 0, 'Tool docs should not be empty'

tools_triage = get_agent_tools_docs('email-triage')
# Different agents should have different tool docs
assert tools != tools_triage, 'Different agents should have different tool docs'

print('All agent_context checks passed')
"
    </automated>
  </verify>
  <done>AgentContext dataclass renders ~200-300 token prompt block with user awareness, temporal context, and action state. get_agent_tools_docs returns per-agent tool documentation (ROUTE-04).</done>
</task>

<task type="auto">
  <name>Task 2: Restructure all three agent prompts for Gemini optimization</name>
  <files>prompts/agent_morning_briefing.md, prompts/agent_email_triage.md, prompts/agent_evening_digest.md</files>
  <action>
Restructure all three agent prompts following Gemini best practices per user decision: "Prompts need restructuring: constraints at TOP, few-shot > verbose instructions."

**For each of the 3 prompt files, apply this structure:**

1. **Constraints section FIRST** (top of file, before anything else):
   - Move existing Grounding Rules section to the very top
   - Add: "Temperature: 1.0. Do not request lower temperature."
   - Add: "Response format: Use Markdown headings and bullet points. Do not mix XML tags with Markdown."
   - Keep existing anti-hallucination rules

2. **Identity and role** (second section):
   - Keep existing agent identity paragraph but make it concise
   - Remove verbose explanations -- Gemini performs better with fewer words

3. **Available tools** (third section):
   - Add a placeholder comment: `<!-- Agent tools injected at runtime by executor -->`
   - This is where get_agent_tools_docs() output will be inserted by the executor

4. **Output format with few-shot example** (fourth section):
   - REPLACE verbose output format instructions with a concrete few-shot example
   - For morning-briefing: Show a realistic example briefing (3-5 lines showing expected sections)
   - For email-triage: Show a realistic example triage output (2-3 classified emails)
   - For evening-digest: Show a realistic example digest (3-5 lines showing expected sections)
   - Keep examples SHORT (~100-200 tokens each) -- they're more effective than paragraphs of instructions

5. **Data instructions LAST** (per Gemini long-context guidance):
   - Move per-section data-availability guidance to the end of the prompt
   - These are instructions about what to do with the live data that follows

**IMPORTANT:** Preserve all existing Grounding Rules content -- only restructure the ORDER of sections and add few-shot examples. Do not remove any anti-hallucination rules. Keep Markdown structure (no XML tags). The content of the grounding rules from Phase 5 Plan 02 must be preserved exactly.
  </action>
  <verify>
    <automated>cd "$HOME/dev/promaia" && python -c "
import re
from pathlib import Path

prompts = ['prompts/agent_morning_briefing.md', 'prompts/agent_email_triage.md', 'prompts/agent_evening_digest.md']
for p in prompts:
    content = Path(p).read_text(encoding='utf-8')

    # Check constraints come first (Grounding Rules near top)
    grounding_pos = content.find('Grounding Rules')
    assert grounding_pos >= 0, f'{p}: Missing Grounding Rules'
    assert grounding_pos < 500, f'{p}: Grounding Rules not at top (pos={grounding_pos})'

    # Check for few-shot example
    assert 'example' in content.lower() or 'Example' in content, f'{p}: Missing few-shot example'

    # Check no XML tags mixed with Markdown
    xml_tags = re.findall(r'<(?!!--)[a-zA-Z][^>]*>', content)
    assert len(xml_tags) == 0, f'{p}: Found XML tags mixed with Markdown: {xml_tags[:3]}'

    # Check anti-hallucination rules preserved
    assert 'hallucin' in content.lower() or 'fabricat' in content.lower() or 'invent' in content.lower(), f'{p}: Anti-hallucination rules missing'

    print(f'{p}: structure OK')

print('All prompt restructuring checks passed')
"
    </automated>
  </verify>
  <done>All three agent prompts restructured with constraints-first, few-shot examples, Markdown-only structure, tool injection placeholder, and all existing Grounding Rules preserved.</done>
</task>

</tasks>

<verification>
- AgentContext can be constructed manually and renders a reasonable prompt block
- AgentContext.load_from_brain() loads real data from Postgres (tested via manual run)
- All 3 agent prompts have Grounding Rules within first 500 chars
- All 3 agent prompts include at least one few-shot example
- No XML tags in any prompt file
- get_agent_tools_docs returns different tool sets for different agents
</verification>

<success_criteria>
1. AgentContext.to_prompt_block() produces ~200-300 token user awareness block
2. get_agent_tools_docs() returns agent-specific tool lists (ROUTE-04 dynamic injection)
3. All prompts restructured: constraints first, few-shot examples, Markdown-only
4. Existing Grounding Rules content preserved verbatim
</success_criteria>

<output>
After completion, create `.planning/phases/06-waste-elimination-spend-visibility/06-02-SUMMARY.md`
</output>
